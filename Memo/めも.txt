


PCに異常が発生したときに役に立つコマンド

Dism.exe /Online /Cleanup-Image /RestoreHealth
sfc /scannow
chkdsk/f



1アンペア = 毎秒 約6.2 * 10^18個の電子



       認証   鍵交換         データ
TLS → RSA  & 楕円曲線暗号 & aes



均一なスケーリング
uniform scaling





go.SetActive(bool)
go が Active かを変更する

go.activeSelf {get;}
go が Active かを返す

go.activeInHierarchy {get;}
すべてのgoの親 と go が Active であれば true を返す



Unity の Frame Debugger の Draw Dynamic は Graphics.Blit のこと



NDC (Normalized Device Coordinates)
クリップ座標を w で割ると出てくる値のこと
xとy が -1 ～ 1 の領域が画面の描画範囲内となる



enable = true, TargetTexture = None, TargetEye = None (MainDisplay)
ピクセル数はリアルタイムで更新される
一部の設定値 (ProjectionMatrix, Aspect) はワールド起動時に計算されるが、それ以降は更新されない (たぶん)
fov は常に 60 になる
デスクトップのモニターに表示されているウィンドウの情報を取得している?
GetStereoProjectionMatrix は初期値を返す

enable = true, TargetTexture = None, TargetEye = Both
ピクセル数はリアルタイムで更新される
GetStereoProjectionMatrix が使える
リアルタイムで更新されるかどうかは不明 (変動させる方法がわからないので検証できない)
m02 と m12 成分に 0 以外の値が入っていることを確認した

enable = true, TargetTexture = 適当なRT, TargetEye = Both
GetStereoProjectionMatrix は初期値を返す
描画がおかしくなる  画面半分だけになったりする



  Unity は 左手座標系 反時計回り
Blender は 右手座標系 時計回り



HDRP/Lit で Emission を使うときは、Exposure weight を下げないと光らない

Shader Graph の HD Scene Color を使うときは、Exposure をオンにすると白くなる現象を回避できる



RenderTexture.active = rt;
と
Graphics.SetRenderTarget(rt);
は同じ



_meshRenderer.material = _material;
と
_meshRenderer.sharedMaterial = _material;
は同じ

_material = _meshRenderer.material;
と
_material = _meshRenderer.sharedMaterial;
は別物



Harmony
Prefixメソッドの戻り値をboolにし、falseを返す事で元メソッドの処理をスキップする事ができる



小さい (2x2?) Cube Render Texture で Auto generate mipmap を オンにして VRChat にアップロードして読み込むとクラッシュすることがある



Assets/WebGLTemplates/{任意の名前}/index.html  内に、
<script src="Test.js"></script>
や
<script src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
を書き込んで、使用するjsファイルを設定する



Unity WebGL で動作確認をするときは、
Compression Format : Disabled
Decompression Fallback : false
に設定するとビルドが早くなる



LIFFブラウザの場合、liff.init()実行時に自動でログイン処理が実行されるため、liff.login()は利用できません。



Build Settings の Platform を WebGL に変更
	↓
WebGL テンプレートを作成
Editor/Data/PlaybackEngines/WebGLSupport/BuildTools/WebGLTemplates/Minimal をコピーするのがおすすめ
	↓
WebGL テンプレート内に必要な js ファイルをぶちこむ
	↓
WebGL テンプレート内の index.html を編集して、使用する js ファイルを設定する
<script src="Test.js"></script>
<script src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
など
	↓
jslib で js と C# をつなげる



liff.shareTargetPicker() で画像やテキストやボタンなどを組み合わせて表示したい場合、
Flex Message Simulator を使って json を生成して、liff.shareTargetPicker() にぶち込むのが良い
https://developers.line.biz/ja/docs/messaging-api/using-flex-messages/



TransformPoint     : 回転とスケールと位置
TransformVector    : 回転とスケール
TransformDirection : 回転



copy /y "$(TargetPath)" "$(ProjectDir)DLL"



Unityで負荷を計測する前に、垂直同期をオンにしてからオフにした方が良いかも
なんか挙動が怪しい



C:\Program Files\Unity\Hub\Editor\2022.3.8f1\Editor\Data\NetStandard\ref\2.1.0\netstandard.dll



GUI.skin.labelなどのGUIStyleを取得する処理を実行するとき、
EditorWindowのOnGUI内とMonoBehaviourのOnGUI内で違う結果になる可能性がある



C#で生成したマテリアルをCommand Bufferで使用するとき、
そのマテリアルの参照をフィールドなどに保持しておく必要がある
そうしないとバグるときがある
GC関係の仕様が影響してるのではないかと予想しているが詳細はよくわからない



AssetBundleは二種類存在する
普通のAssetBundleとStreamedSceneAssetBundle









